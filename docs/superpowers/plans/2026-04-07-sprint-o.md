# Sprint O — Guías de Resolución en Desafíos

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar guías de resolución paso a paso (3 pasos, sin revelar respuesta) a los 16 desafíos PBL existentes, renderizadas como sección expandible en la UI.

**Architecture:** Datos en `challenges.json` (campo `resolution_guide: string[3]`), renderizado en dos componentes existentes (`ChallengeSimulator` para ch1–ch4 y `DirectedChallengeView` para ch5–ch16) en `ModuleChallenges.jsx`.

**Tech Stack:** React (JSX), JSON data, inline styles (tema `C` de `theme.js`).

**Nota:** PED-006 (feedback incorrectas) y GAP-001 (desafíos M5/M6/M7) ya estaban implementados — verificado en esta sesión.

---

### Task 1: Agregar `resolution_guide` a los 16 desafíos en challenges.json

**Files:**
- Modify: `frontend/src/data/challenges.json`

- [ ] **Step 1: Agregar `resolution_guide` a ch1–ch4 (M1)**

En `frontend/src/data/challenges.json`, agregar después del campo `"hint"` de cada desafío ch1–ch4:

**ch1 — Decline Misterioso:**
```json
"resolution_guide": [
  "En M1 Simulador, cargá Pr=2200, Pb=1800, IP=0.18, prof=2400m, Pwh=130 y freq=60 Hz.",
  "Observá el drawdown en las métricas. ¿Supera el 70%? ¿Pwf está cerca de Pb?",
  "Probá reducir la frecuencia a 50-55 Hz y observá cómo baja el drawdown sin perder demasiado caudal."
],
```

**ch2 — Gas Libre en Succión:**
```json
"resolution_guide": [
  "En M1 Simulador, cargá los parámetros del pozo y observá si Pwf < Pb en las métricas.",
  "Activá BSW=40% en el slider y observá cómo cambia la densidad efectiva y el punto de operación.",
  "Probá subir la frecuencia a 65-70 Hz para elevar la presión de succión y reducir el gas libre."
],
```

**ch3 — Surging por Sobredimensionamiento:**
```json
"resolution_guide": [
  "En M1 Simulador, cargá los parámetros y observá el % del BEP en las métricas a 65 Hz.",
  "Si el % BEP supera 132%, estás en zona de surging. Reducí la frecuencia gradualmente.",
  "Buscá la frecuencia donde el % BEP quede entre 68% y 132% — esa es la ventana segura."
],
```

**ch4 — Optimización de Producción:**
```json
"resolution_guide": [
  "Empezá en 50 Hz y subí la frecuencia de a 1 Hz. En cada paso, anotá drawdown y % BEP.",
  "Detenete cuando el drawdown se acerque al 65% o cuando aparezca una alerta de warning/danger.",
  "La frecuencia óptima es la más alta donde drawdown ≤ 65% y BEP entre 68%–132% sin alertas."
],
```

- [ ] **Step 2: Agregar `resolution_guide` a ch5–ch6 (M2)**

**ch5 — TDH Insuficiente:**
```json
"resolution_guide": [
  "Abrí M2 Simulador. Cargá Q=180 m³/d, profundidad=3200m, Pwh=150 psi, tubing 2.992\".",
  "Observá el TDH total calculado en las métricas del simulador.",
  "Dividí TDH_total / H_etapa (35 m) para obtener el número mínimo de etapas. ¿Supera 48?"
],
```

**ch6 — Degradación por Viscosidad:**
```json
"resolution_guide": [
  "Abrí M2 Simulador. Ingresá viscosidad=85 cP y los demás parámetros del pozo.",
  "Observá los factores de corrección HI: CH (cabeza), CQ (caudal) y CE (eficiencia).",
  "Si CH < 0.85, la degradación del 30% es normal para esa viscosidad — no es falla mecánica."
],
```

- [ ] **Step 3: Agregar `resolution_guide` a ch7–ch8 (M3)**

**ch7 — Gas Lock Inminente:**
```json
"resolution_guide": [
  "Abrí M3 Simulador. Cargá GOR=320 m³/m³, Pb=2100 psi y los parámetros del pozo.",
  "Observá el GVF calculado en succión. ¿Supera el 15%? Ese es el umbral de gas lock.",
  "Probá activar el separador AGS rotativo y verificá si el GVF efectivo baja a < 15%."
],
```

**ch8 — Emulsión Severa:**
```json
"resolution_guide": [
  "Abrí M3 Simulador. Ingresá viscosidad=450 cP (la emulsión, no el crudo puro).",
  "Observá el factor CE (eficiencia HI). ¿Es menor a 0.55?",
  "Con CE < 0.55 la bomba consume 2× más potencia por barril. Evaluá: rompe-emulsión o bomba especial."
],
```

- [ ] **Step 4: Agregar `resolution_guide` a ch9–ch10 (M4)**

**ch9 — Caída de Voltaje Crítica:**
```json
"resolution_guide": [
  "Abrí M4 Simulador. Cargá los datos del cable (longitud, calibre AWG, temperatura).",
  "Observá la caída de voltaje en % respecto al voltaje del motor. ¿Supera el 5%?",
  "Probá cambiar el calibre AWG a uno más grueso (número menor) hasta que V_drop < 5%."
],
```

**ch10 — THD fuera de norma:**
```json
"resolution_guide": [
  "Abrí M4 Simulador. Revisá el THD de la topología VSD actual del pozo.",
  "Compará con el límite IEEE 519: THDv < 5% en el PCC.",
  "Evaluá qué topología (18 pulsos, AFE) cumple la norma y comparalo con el costo relativo."
],
```

- [ ] **Step 5: Agregar `resolution_guide` a ch11–ch12 (M5)**

**ch11 — Carta Amperiométrica:**
```json
"resolution_guide": [
  "Abrí M5 pestaña Teoría. Buscá la sección de cartas amperiométricas típicas.",
  "Compará: surging = oscilación periódica y simétrica; gas lock = caída brusca asimétrica a cero.",
  "Con oscilación ±20% cíclica, ¿el patrón se parece más a surging o a gas lock?"
],
```

**ch12 — Vibración Creciente:**
```json
"resolution_guide": [
  "Abrí M5 pestaña Teoría. Buscá los umbrales de vibración recomendados (mm/s RMS).",
  "Compará 5.8 mm/s con el umbral de alerta (4 mm/s) y el de paro.",
  "Identificá las causas mecánicas típicas de vibración alta: desbalanceo, rodamiento, cavitación."
],
```

- [ ] **Step 6: Agregar `resolution_guide` a ch13–ch14 (M6)**

**ch13 — DIFA Código 4900:**
```json
"resolution_guide": [
  "Abrí M6 pestaña Teoría o Simulador. Buscá el código 4900 en la tabla de códigos API RP 11S1.",
  "Trazá la cadena causal: T° motor (178°C) > T° límite (150°C) → degradación de sello → invasión.",
  "¿Qué variable operativa debió tener un set point más estricto para prevenir la falla?"
],
```

**ch14 — Reincidencia de Falla:**
```json
"resolution_guide": [
  "Abrí M6 y revisá qué tipo de falla produce rotura torsional de eje repetida.",
  "Evaluá dos hipótesis: (1) sólidos bloqueando el eje, (2) arranque directo sin soft-start.",
  "¿El VSD tiene perfil de aceleración gradual (ramp-up)? ¿El diámetro de eje es suficiente?"
],
```

- [ ] **Step 7: Agregar `resolution_guide` a ch15–ch16 (M7)**

**ch15 — MTBF Insuficiente:**
```json
"resolution_guide": [
  "Abrí M7 Simulador. Usá la fórmula R(t) = e^(-t/MTBF) con R=0.85 y t=365 días.",
  "Despejá: MTBF = -t / ln(R) = -365 / ln(0.85).",
  "Compará el MTBF requerido con el promedio histórico de 200 días. ¿Es alcanzable?"
],
```

**ch16 — Datos Censurados:**
```json
"resolution_guide": [
  "Abrí M7 pestaña Teoría. Revisá la sección de datos censurados y MLE.",
  "Calculá el MTBF sin censurar (solo fallos) vs. con censura (sumando días de equipos vivos).",
  "¿Cuánto cambia el MTBF al incluir los equipos que siguen operando?"
],
```

- [ ] **Step 8: Verificar JSON válido**

Run: `cd "C:\Users\Pablo\Desktop\Proyectos\SIM\SimBes\frontend" && node -e "JSON.parse(require('fs').readFileSync('src/data/challenges.json','utf8')); console.log('JSON OK')"`
Expected: `JSON OK`

---

### Task 2: Renderizar `resolution_guide` en DirectedChallengeView

**Files:**
- Modify: `frontend/src/components/challenges/ModuleChallenges.jsx`

- [ ] **Step 1: Agregar estado `showGuide` en DirectedChallengeView**

En `DirectedChallengeView` (línea ~335), agregar estado junto a los existentes:

```jsx
const [showGuide,   setShowGuide]   = useState(false);
```

- [ ] **Step 2: Renderizar sección expandible entre Hint y Botones**

Después del bloque `{/* Hint */}` (después de línea ~379) y antes de `{/* Botones */}` (línea ~382), insertar:

```jsx
{/* Resolution Guide (PED-007) */}
{ch.resolution_guide && ch.resolution_guide.length > 0 && (
  <div style={{ marginBottom: 20 }}>
    <button
      onClick={() => setShowGuide(g => !g)}
      style={{
        background: showGuide ? ch.color + '15' : 'transparent',
        border: `1px solid ${ch.color}40`,
        borderRadius: 8,
        color: ch.color,
        fontSize: 11,
        fontFamily: C.fontUI,
        fontWeight: 600,
        padding: '8px 16px',
        cursor: 'pointer',
        width: '100%',
        textAlign: 'left',
      }}
    >
      {showGuide ? '▾' : '▸'} 🗺️ Guía de resolución (sin spoilers)
    </button>
    {showGuide && (
      <div style={{
        background: ch.color + '08',
        border: `1px solid ${ch.color}30`,
        borderTop: 'none',
        borderRadius: '0 0 8px 8px',
        padding: '14px 18px',
      }}>
        {ch.resolution_guide.map((step, i) => (
          <div key={i} style={{
            display: 'flex',
            gap: 10,
            alignItems: 'flex-start',
            marginBottom: i < ch.resolution_guide.length - 1 ? 10 : 0,
          }}>
            <span style={{
              background: ch.color + '25',
              color: ch.color,
              fontFamily: C.font,
              fontSize: 10,
              fontWeight: 700,
              width: 22,
              height: 22,
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>{i + 1}</span>
            <span style={{
              fontSize: 11,
              color: C.text,
              fontFamily: C.fontUI,
              lineHeight: 1.7,
            }}>{step}</span>
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

- [ ] **Step 3: Verificar build**

Run: `cd "C:\Users\Pablo\Desktop\Proyectos\SIM\SimBes\frontend" && npx vite build 2>&1 | tail -5`
Expected: Build sin errores.

---

### Task 3: Renderizar `resolution_guide` en ChallengeSimulator

**Files:**
- Modify: `frontend/src/components/challenges/ModuleChallenges.jsx`

- [ ] **Step 1: Agregar estado `showGuide` en ChallengeSimulator**

En `ChallengeSimulator` (línea ~203), agregar junto a `showExpl`:

```jsx
const [showGuide, setShowGuide] = useState(false);
```

- [ ] **Step 2: Renderizar sección expandible después de la Pista**

En `ChallengeSimulator`, después del bloque `{/* Problem statement */}` que contiene la pista (línea ~233, cierre del div con `ch.hint`), insertar antes de `{/* Fixed params banner */}`:

```jsx
{/* Resolution Guide (PED-007) */}
{ch.resolution_guide && ch.resolution_guide.length > 0 && (
  <div>
    <button
      onClick={() => setShowGuide(g => !g)}
      style={{
        background: showGuide ? ch.color + '15' : 'transparent',
        border: `1px solid ${ch.color}40`,
        borderRadius: 8,
        color: ch.color,
        fontSize: 11,
        fontFamily: C.fontUI,
        fontWeight: 600,
        padding: '8px 16px',
        cursor: 'pointer',
        width: '100%',
        textAlign: 'left',
      }}
    >
      {showGuide ? '▾' : '▸'} 🗺️ Guía de resolución (sin spoilers)
    </button>
    {showGuide && (
      <div style={{
        background: ch.color + '08',
        border: `1px solid ${ch.color}30`,
        borderTop: 'none',
        borderRadius: '0 0 8px 8px',
        padding: '14px 18px',
      }}>
        {ch.resolution_guide.map((step, i) => (
          <div key={i} style={{
            display: 'flex',
            gap: 10,
            alignItems: 'flex-start',
            marginBottom: i < ch.resolution_guide.length - 1 ? 10 : 0,
          }}>
            <span style={{
              background: ch.color + '25',
              color: ch.color,
              fontFamily: C.font,
              fontSize: 10,
              fontWeight: 700,
              width: 22,
              height: 22,
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>{i + 1}</span>
            <span style={{
              fontSize: 11,
              color: C.text,
              fontFamily: C.fontUI,
              lineHeight: 1.7,
            }}>{step}</span>
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

- [ ] **Step 3: Build final**

Run: `cd "C:\Users\Pablo\Desktop\Proyectos\SIM\SimBes\frontend" && npx vite build 2>&1 | tail -5`
Expected: Build sin errores.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/data/challenges.json frontend/src/components/challenges/ModuleChallenges.jsx
git commit -m "feat(PED-007): add resolution_guide to all 16 challenges with expandable UI"
```
